# 任务清单
1. 每个子agent返回格式
2. audit agent返回格式
3. 每个子agent需要返回搜索的文档片段，这些文档片段是audit agent的判断的主要依据
4. 为每个子agent配置RAG
5. audit agent的路由函数
```python
def auditor_router(state: State):
    feedback = state.get("audit_feedback")
    if feedback:
        # 如果有审计意见，打回给 Supervisor 重新规划或重新检索
        return "supervisor"
    # 审计通过，结束图运行
    return END
```