# 三端前端实现验收

## 实现范围

当前前端共实现 28 类实际页面、35 条可访问路由，覆盖公共登录、用户端、企业端和平台管理端。13 张桌面设计图中的“安全审计与系统运行”被拆成安全、运维、模型三个独立页面，并增加只读平台配置页；通知、帮助和个人资料按三端权限复用，共形成 9 条路由；任务、证据、文档、智能体、评测、成员、审计事件、租户运行和服务探针详情页补齐列表后的核验流程。

| 端 | 路由 | 页面 | 数据来源 |
| --- | --- | --- | --- |
| 公共 | `/login` | 登录与租户选择 | 开发令牌或正式 JWT/OIDC |
| 用户端 | `/app` | AI 工作台 | 本地会话状态、真实聊天接口 |
| 用户端 | `/app/chat/:threadId` | 多智能体回答 | `/api/chat` SSE 流 |
| 用户端 | `/app/search` | 企业搜索与证据 | `/api/search` 混合检索 |
| 用户端 | `/app/search/evidence/:evidenceId` | 检索证据详情 | 当前租户和用户作用域的 Redis 短期缓存 |
| 用户端 | `/app/tasks` | 任务与追问 | SQL 会话目录与旧版审计终态合并 |
| 用户端 | `/app/tasks/:taskId` | 任务详情与会话记录 | `/api/tasks/{task_id}`，租户与用户双重隔离 |
| 用户端 | `/app/documents` | 我的文档 | `/api/documents`、上传与入库接口 |
| 用户端 | `/app/documents/:documentId` | 文档详情与解析预览 | `/api/documents/{document_id}`，owner/ACL 授权 |
| 用户端 | `/app/notifications`、`/app/help`、`/app/profile` | 通知、帮助、个人资料 | 任务与文档聚合、健康探针、JWT 声明 |
| 企业端 | `/enterprise` | 企业运营总览 | 租户审计、文档和依赖探针 |
| 企业端 | `/enterprise/knowledge` | 知识与解析 | 租户文档、上传和解析状态 |
| 企业端 | `/enterprise/knowledge/:documentId` | 企业文档详情 | `/api/admin/documents/{document_id}` 与受控解析预览 |
| 企业端 | `/enterprise/agents` | 智能体与 MCP | 智能体枚举与 MCP 启动配置 |
| 企业端 | `/enterprise/agents/:agentId` | 智能体运行详情 | 当前发布代码、领域注册表、中间件参数与 MCP 启动配置 |
| 企业端 | `/enterprise/members` | 成员与权限 | JWT 声明与审计中已观测身份 |
| 企业端 | `/enterprise/members/:actorId` | 已观测成员详情 | 当前 JWT 声明与最多 200 条租户作用域审计事件 |
| 企业端 | `/enterprise/evaluation` | 评测与成本 | 版本库中的固定评测结果文件 |
| 企业端 | `/enterprise/evaluation/:runId` | 评测运行详情 | 四组版本化 `metrics.json` 的方案与指标明细 |
| 企业端 | `/enterprise/notifications`、`/enterprise/help`、`/enterprise/profile` | 通知、帮助、个人资料 | 租户总览与探针、健康探针、JWT 声明 |
| 管理端 | `/admin/tenants` | 租户与配额 | 当前 JWT 租户、审计、文档和全局限制 |
| 管理端 | `/admin/tenants/:tenantId` | 租户运行详情 | 当前 JWT 租户的文档与最多 200 条审计事件聚合 |
| 管理端 | `/admin/security` | 安全审计 | `/api/admin/audit-events` |
| 管理端 | `/admin/security/:eventId` | 审计事件详情 | 租户作用域事件主键与同 `request_id` 请求链 |
| 管理端 | `/admin/operations` | 系统运行 | 健康探针与 Worker 启动策略 |
| 管理端 | `/admin/operations/services/:serviceName` | 服务探针详情 | 已注册探针定义与单服务实时检查结果 |
| 管理端 | `/admin/models` | 模型与容量 | Ollama `/api/tags` |
| 管理端 | `/admin/settings` | 平台配置 | 启动配置与环境变量的脱敏只读视图 |
| 管理端 | `/admin/notifications`、`/admin/help`、`/admin/profile` | 通知、帮助、个人资料 | 运行与审计聚合、健康探针、JWT 声明 |

## 已实现动态效果

- 用户问答按 SSE 内容块流式追加，提供停止、引用、附件和终态反馈。
- 用户工作台快捷任务分批进入，输入聚焦和拖拽附件具有状态反馈。
- 搜索加载、证据选择、任务刷新、文档上传与入库进度保持稳定布局。
- 检索结果可进入独立证据详情并在 TTL 内刷新恢复；文档详情支持 ACL 复核、解析预览和真实后端状态。
- 历史会话可从任意浏览器直接恢复用户可见消息；LangGraph 检查点只负责执行状态，不向页面暴露系统或工具消息。
- 回答页支持真实复制、Markdown 导出、反馈提交和再次提问；引用未返回定位地址时“打开原文”保持明确禁用。
- 三端侧栏在移动端使用遮罩抽屉，关闭后不改变页面内容宽度。
- 企业端指标刷新、会话状态条、解析流水线和评测基线对比均由真实数据驱动。
- 智能体卡片可进入只读运行详情，区分 MCP 配置与实际探针状态；评测来源可进入运行详情并横向核对全部候选方案。
- 成员身份和审计事件可进入独立详情；移动端事件行提供明确的详情箭头，详情刷新重新执行服务端租户作用域查询。
- 管理端筛选审计事件时只更新结果区，并支持游标加载更早事件。
- 租户列表可进入独立运行详情，跨租户路径返回 404；服务列表可逐项执行实时探针并查看成功条件与超时。
- 顶部通知计数来自实时状态聚合，通知筛选与刷新不改变列表宽度；帮助页在就绪接口返回 503 时仍显示各依赖真实状态。
- 桌面侧栏可收起为图标导航；三端顶部搜索、租户、通知、帮助和个人资料入口均可到达实际页面。
- 全局支持 `prefers-reduced-motion: reduce`，降低动态偏好下取消位移和循环动画。

## 验证方法

- 后端：在 Conda `rag` 环境运行 `python -m pytest -q`，当前 98 项通过；包含空库迁移、会话租户隔离、SSE 终态持久化、证据跨租户/跨用户隔离、文档 ACL、智能体清单、评测结果脱敏、成员声明边界、审计追踪隔离、租户详情隔离、单服务探针、就绪状态聚合和依赖故障映射测试。
- Python 静态检查：运行 Ruff，平台与企业接口、测试和主应用无错误。
- 前端：运行 `npm run typecheck` 和 `npm run build`，TypeScript 与 Vite 生产构建通过。
- 数据库与容器：全新 SQLite 从 Alembic `0001` 升级到 `20260902_0003`，会话三表创建成功；使用一次性占位环境变量执行 `docker compose config --quiet` 通过。
- 视觉回归：在真实本地服务上逐页访问，桌面使用 1280 x 720，移动使用 390 x 844；任务、证据及用户/企业文档详情均无横向溢出，解析正文在移动端转为单列阅读。
- 数据真实性：Redis、Milvus、Neo4j、Ollama、MCP 和数据库均使用实时探针；租户、审计、文档、模型和评测不使用随机数或静态演示记录。

## 当前边界

- 项目尚无平台级租户目录、计费系统和跨租户配额执行器，因此管理端只展示当前令牌租户；创建租户和调整配额保持禁用。
- 租户运行详情中的审计分布最多覆盖最近 200 条事件；服务探针详情没有时序历史，不能用于声称 SLA 或长期可用率。
- 项目尚无 SCIM 目录同步，因此企业成员页只展示 JWT 当前身份和审计窗口内已观测身份；邀请成员保持禁用。
- 已观测成员详情不会推断非当前身份的角色、组或权限；接入企业 IdP/SCIM 前不能把该页面当作完整成员目录。
- Ollama 不提供 GPU 显存、QPS 和队列指标，模型页不推测容量；需要接入 GPU Exporter 和指标存储后再启用容量告警。
- 维护操作需要审批、后台任务、结果审计和回滚链路，当前保持禁用，不能直接宣称满足大规模生产控制面要求。
- 会话正文现已持久化到业务数据库；生产部署必须配置数据库静态加密、备份访问控制、法务确认的保留期和按用户请求删除流程。
- 检索证据默认只在 Redis 保存 15 分钟并绑定租户与用户；原始文件下载和 PDF 页码/坐标高亮尚未实现。
- 本次本地检索回归识别到 Ollama 未运行，接口将明确的连接或超时故障映射为可观测 503 并写入脱敏失败审计；证据详情通过真实上传文档和项目缓存函数验证。
- 当前浏览器验收覆盖主要桌面与移动视口；正式上线前仍需补充多浏览器、键盘全流程、屏幕阅读器和高并发压测。
